add_library(LocationParser STATIC LocationParser.cpp)
target_include_directories(LocationParser PUBLIC
  "${CMAKE_SOURCE_DIR}/include"
  ${TERMCOLOR_INCLUDE_DIRS}
)
if (BUILD_COVERAGE)
  target_compile_options(LocationParser PRIVATE -coverage)
  target_link_options(LocationParser PRIVATE -coverage)
  addCoverage(LocationParser_coverage)
endif()
