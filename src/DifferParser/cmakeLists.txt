add_library(DifferParser STATIC DifferParser.cpp)
target_include_directories(DifferParser PUBLIC
  "${CMAKE_SOURCE_DIR}/include"
  ${TERMCOLOR_INCLUDE_DIRS}
)
if (BUILD_COVERAGE)
  target_compile_options(DifferParser PRIVATE -coverage)
  target_link_options(DifferParser PRIVATE -coverage)
  addCoverage(DifferParser_coverage)
endif()
