add_library(assist STATIC help.cpp)
target_include_directories(assist PUBLIC
  ${TERMCOLOR_INCLUDE_DIRS}
)
if (BUILD_COVERAGE)
  target_compile_options(assist PRIVATE -coverage)
  target_link_options(assist PRIVATE -coverage)
  addCoverage(assist_coverage)
endif()
